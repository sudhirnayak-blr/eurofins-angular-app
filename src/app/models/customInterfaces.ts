export interface IProductActions { 
    productId: number, 
    actionType: number
}

//Goto the Models folder and add a new TS file called customInterfaces.ts 
//This interface will be used to map the productID and the actions in, 
// both the productsHome component (ts and html) and also in the ProductsList component 
//  (ts and html)
